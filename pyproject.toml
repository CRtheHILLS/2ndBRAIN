[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secondbrain"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115", "uvicorn[standard]>=0.30", "python-multipart>=0.0.9",
  "pydantic-settings>=2.4", "anthropic>=0.40", "httpx>=0.27",
  "watchdog>=5.0", "pymupdf>=1.24", "pillow>=10.4", "pillow-heif>=0.18", "jinja2>=3.1",
  "python-frontmatter>=1.1", "sqlite-vec>=0.1.6", "markdown>=3.7", "openpyxl>=3.1",
]
[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "httpx>=0.27"]
notebooklm = ["notebooklm-py>=0.1"]
[project.scripts]
brain-sync = "sync.watcher:main"
brain-nblm = "sync.notebooklm_import:main"
[tool.pytest.ini_options]
testpaths = ["tests"]
[tool.setuptools.packages.find]
include = ["brain*", "api*", "sync*"]
[tool.setuptools.package-data]
brain = ["templates/*.j2", "static/*.css"]
